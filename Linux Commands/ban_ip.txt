#!/bin/bash
if [ "$0" != "" ]; then
  echo "First Parameter is missing, please input IP"
else
  echo 'Banning IP = ' $0
  sudo iptables -A INPUT -s $0 -j DROP
fi

#View the Banned IP
sudo iptables -L -v
