#!/bin/bash
if [ "$1" != "" ]; then
  echo "First Parameter is missing, please input IP"
else
  echo 'Banning IP = ' $1
  sudo iptables -A INPUT -s $1 -j DROP
fi

#View the Banned IP
sudo iptables -L -v
